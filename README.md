# Commercial Psychology OS v4

GitHubの既存リポジトリで `index.html` をこの版へ置き換えてください。

- `functions/api/state.js` はCloudflare D1同期APIです。
- `schema.sql` は既存D1を初期化済みなら再実行不要です。
- PagesのD1 binding名は `DB` にしてください。
- GitHubへCommitするとCloudflare Pagesが自動再デプロイします。
